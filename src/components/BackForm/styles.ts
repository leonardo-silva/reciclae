import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        width: '100%',
        height: 500,
        borderRadius: 77,
        flexDirection: 'column',
        paddingTop: 10
    }
})